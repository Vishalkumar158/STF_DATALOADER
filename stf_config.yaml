# CLASS_NAMES: ['PassengerCar', 'Pedestrian', 'LargeVehicle']

# DATA_CONFIG:
#     POINT_CLOUD_RANGE: [0, -40, -3, 70.4, 40, 1]   # [x_min, y_min, z_min, x_max, y_max, z_max]

#     DATA_SPLIT:
#         train: train      # Uses ImageSets/train_FOV3000.txt
#         val: val          # Uses ImageSets/val_FOV3000.txt

#     INFO_PATH:
#         train: []
#         val: []

#     FOV_POINTS_ONLY: True         # Keep only points inside camera FOV during loading/training
#     DROP_EMPTY_ANNOTATIONS: True  # Skip frames with no objects from CLASS_NAMES

# DATA_AUGMENTOR:
#     DISABLE_LIST: []              # You can disable augmentations here if needed later


# CLASS_NAMES: ['Car', 'Pedestrian', 'Cyclist']
# POINT_CLOUD_RANGE: [0, -40, -3, 70.4, 40, 1]
# POINT_FEATURE_ENCODING:
#     used_feature_list: ['x', 'y', 'z', 'intensity', 'elongation']
#     src_feature_list: ['x', 'y', 'z', 'intensity', 'elongation']
#     encoded_feature_dim: 5

# DATA_CONFIG:
#     _BASE_CONFIG_: null   # optional, not needed unless you have a base

#     DATA_SPLIT:
#         train: train_FOV3000
#         val: val_FOV3000

#     INFO_PATH:
#         train: []
#         val: []

#     FOV_POINTS_ONLY: True
#     DROP_EMPTY_ANNOTATIONS: True

# DATA_AUGMENTOR:
#     DISABLE_LIST: []

# DATA_PROCESSOR:
#     - NAME: transform_points_to_voxels
#       VOXEL_SIZE: [0.05, 0.05, 0.1]
#       MAX_POINTS_PER_VOXEL: 5
#       MAX_NUMBER_OF_VOXELS:
#         train: 160000
#         test: 160000
#       NUM_POINT_FEATURES: 5


CLASS_NAMES: ['Car', 'Pedestrian', 'Cyclist']

POINT_CLOUD_RANGE: [0, -40, -3, 70.4, 40, 1]

POINT_FEATURE_ENCODING:
    used_feature_list: ['x', 'y', 'z', 'intensity', 'elongation']
    src_feature_list: ['x', 'y', 'z', 'intensity', 'elongation']
    encoded_feature_dim: 5

NUM_POINT_FEATURES: 5

DATA_SPLIT:                   
    train: train
    val: val

DATA_CONFIG:
    INFO_PATH:
        train: []
        val: []

    FOV_POINTS_ONLY: True
    DROP_EMPTY_ANNOTATIONS: True

DATA_AUGMENTOR:
    DISABLE_LIST: []

DATA_PROCESSOR:
    - NAME: transform_points_to_voxels
      VOXEL_SIZE: [0.05, 0.05, 0.1]
      MAX_POINTS_PER_VOXEL: 5
      MAX_NUMBER_OF_VOXELS:
        train: 160000
        test: 160000